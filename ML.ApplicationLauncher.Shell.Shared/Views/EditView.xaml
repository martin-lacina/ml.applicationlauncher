<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ML.ApplicationLauncher.Shell.Shared.ViewModels"
             x:Class="ML.ApplicationLauncher.Shell.Shared.Views.EditView">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:CommandProcessViewModel}">
            <StackPanel Margin="8">
                <TextBlock Text="Process Details" FontWeight="Bold" />
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Name:" Width="60" />
                    <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="300" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock Text="Path:" Width="60" />
                    <TextBox Text="{Binding Path, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="300" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock Text="Args:" Width="60" />
                    <TextBox Text="{Binding Arguments, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="300" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:CommandGroupViewModel}">
            <StackPanel Margin="8">
                <TextBlock Text="Group Details" FontWeight="Bold" />
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Name:" Width="60" />
                    <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="300" />
                </StackPanel>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>

    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <ToggleButton Content="Edit" IsChecked="{Binding IsEditMode, Mode=TwoWay}" ToolTip="Toggle edit mode" />
            <Button Content="Add Group" Command="{Binding AddGroupCommand}" />
            <Button Content="Add Process" Command="{Binding AddProcessCommand}" />
            <Button Content="Remove" Command="{Binding RemoveCommand}" />
            <Button Content="Move Up" Command="{Binding MoveUpCommand}" />
            <Button Content="Move Down" Command="{Binding MoveDownCommand}" />
            <Separator />
            <Button Content="Save" Command="{Binding SaveCommand}" />
            <Button Content="Undo" Command="{Binding UndoCommand}" />
            <Button Content="Redo" Command="{Binding RedoCommand}" />
        </ToolBar>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <TreeView Grid.Column="0" ItemsSource="{Binding Groups}" SelectedItemChanged="TreeView_SelectedItemChanged">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Name}" />
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*" />
                    <RowDefinition Height="2*" />
                </Grid.RowDefinitions>

                <ListView x:Name="ProcessesListView" Grid.Row="0" ItemsSource="{Binding SelectedGroup.Processes}" SelectedItem="{Binding SelectedProcess, Mode=TwoWay}"
                          AllowDrop="True"
                          PreviewMouseLeftButtonDown="ProcessesListView_PreviewMouseLeftButtonDown"
                          MouseMove="ProcessesListView_MouseMove"
                          Drop="ProcessesListView_Drop">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" />
                            <GridViewColumn Header="Path" DisplayMemberBinding="{Binding Path}" />
                            <GridViewColumn Header="Args" DisplayMemberBinding="{Binding Arguments}" />
                        </GridView>
                    </ListView.View>
                </ListView>

                <ContentControl Grid.Row="1" Content="{Binding SelectedDetail}" />
            </Grid>
        </Grid>
    </DockPanel>
</UserControl>
