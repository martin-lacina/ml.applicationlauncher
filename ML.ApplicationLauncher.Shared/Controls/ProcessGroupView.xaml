<ContentControl x:Class="ML.ApplicationLauncher.Shared.Controls.ProcessGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:ML.ApplicationLauncher.Shared.ViewModels"
             xmlns:controls="clr-namespace:ML.ApplicationLauncher.Shared.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewModels:CommandGroupViewModel, IsDesignTimeCreatable=False}">
    <GroupBox Header="{Binding}" Margin="0,0,0,5">
        <GroupBox.HeaderTemplate>
            <DataTemplate>
                <controls:StartButtonControl />
            </DataTemplate>
        </GroupBox.HeaderTemplate>
        <Grid MaxWidth="230">
            <Grid.RowDefinitions>
                <RowDefinition Height="5" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="1">
                <!-- Nested groups -->
                <ItemsControl ItemsSource="{Binding Path=Children, Mode=OneWay}">
                    <ItemsControl.Resources>
                        <DataTemplate DataType="{x:Type viewModels:CommandGroupViewModel}">
                            <controls:ProcessGroupView />
                        </DataTemplate>
                    </ItemsControl.Resources>
                </ItemsControl>
                <!-- Processes -->
                <ItemsControl ItemsSource="{Binding Path=Processes, Mode=OneWay}">
                    <ItemsControl.Resources>
                        <DataTemplate DataType="{x:Type viewModels:CommandProcessViewModel}">
                            <controls:StartButtonControl />
                        </DataTemplate>
                    </ItemsControl.Resources>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </GroupBox>
</ContentControl>
